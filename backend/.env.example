# Environment Configuration Example
# Copy this file to .env and fill in your actual values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Node environment (development, production, test)
NODE_ENV=development

# Server port (5000 for local development, 10000 for Render)
PORT=5000

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# MongoDB Connection String
# Local development example:
# MONGODB_URI=mongodb://localhost:27017/community_platform

# MongoDB Atlas example (recommended for production):
# MONGODB_URI=mongodb+srv://username:password@clustername.xxxxx.mongodb.net/community_platform

MONGODB_URI=mongodb://localhost:27017/community_platform

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================

# JWT Secret Key (use a strong, random string in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT Token Expiration (e.g., '7d', '24h', '1h')
JWT_EXPIRES_IN=7d

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Frontend URL for CORS (local development)
FRONTEND_URL=http://localhost:5173

# Production Frontend URL (for deployed apps)
PRODUCTION_FRONTEND_URL=https://your-frontend-app.onrender.com

# CORS Origins (comma-separated list)
CORS_ORIGIN=http://localhost:5173

# Production CORS Origin
PRODUCTION_CORS_ORIGIN=https://your-frontend-app.onrender.com

# =============================================================================
# RATE LIMITING
# =============================================================================

# Rate limit: maximum requests per window
RATE_LIMIT_MAX=100

# Rate limit window in minutes
RATE_LIMIT_WINDOW=15

# =============================================================================
# EMAIL CONFIGURATION (Optional - for future features)
# =============================================================================

# Email service configuration
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# =============================================================================
# FILE UPLOAD CONFIGURATION (Optional - for future features)
# =============================================================================

# Maximum file size in MB
# MAX_FILE_SIZE=5

# Allowed file types (comma-separated)
# ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================

# Enable detailed logging in development
DEBUG=true

# Database connection logging
DB_LOGGING=false

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================

# For Render deployment:
# 1. Set NODE_ENV=production
# 2. Use PORT=10000 (Render assigns this automatically)
# 3. Set MONGODB_URI to your MongoDB Atlas connection string
# 4. Generate a strong JWT_SECRET
# 5. Set FRONTEND_URL and CORS_ORIGIN to your deployed frontend URL
# 6. Set all security-related variables

# Example production values:
# NODE_ENV=production
# PORT=10000
# MONGODB_URI=mongodb+srv://admin:password@communityplatform.vbs6dyv.mongodb.net/community_platform
# JWT_SECRET=a-very-long-random-string-generated-securely
# FRONTEND_URL=https://minilinkdin.onrender.com
# CORS_ORIGIN=https://minilinkdin.onrender.com